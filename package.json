{
  "name": "procrastinot",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev:oracle": "pnpm --filter oracle dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "test:contracts": "cd contracts && forge test -vvv",
    "sync:abi": "tsx scripts/sync-abi.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
