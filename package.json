{
  "name": "procrastinot",
  "private": true,
  "description": "On-chain commitment app with oracle-judged evidence and Degen Mode escrow.",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev:oracle": "pnpm --filter oracle dev",
    "dev:indexer": "pnpm --filter indexer dev",
    "dev:web": "pnpm --filter web dev",
    "plinko:demo": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "test:contracts": "cd contracts && forge test -vvv",
    "test:web": "pnpm --filter web test",
    "sync:abi": "tsx scripts/sync-abi.ts",
    "distribute:env": "tsx scripts/distribute-supabase-env.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
